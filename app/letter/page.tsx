import { Book } from "@/components/letter/Book";

export default function LetterPage() {
    return (
        <div className="flex-1 w-full flex flex-col items-center justify-center p-4 max-w-7xl mx-auto overflow-hidden">
            <Book />
        </div>
    );
}
